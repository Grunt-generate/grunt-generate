/*global Backbone */
var app = app || {};
(function(){
  'use strict';
  app.routers.<%= meta.className %> = Backbone.Router.extend({
    routes : {
    },
    initialize : function(){
    }
  });
})();