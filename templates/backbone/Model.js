/*global Backbone */
var app = app || {};
(function(){
  'use strict';
  app.models.<%= meta.className %> = Backbone.Model.extend({
    defaults : {
    },
    initialize  : function(){
    }
  });
})();