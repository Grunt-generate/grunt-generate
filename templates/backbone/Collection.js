/*global Backbone */
var app = app || {};
(function(){
  'use strict';
  app.collections.<%= meta.className %> = Backbone.Collection.extend({
//    model : undefined,
//    url : undefined,
    initialize : function(){
    }
  });
})();